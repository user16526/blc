---
description: Run the full Idea→Spec→Plan→Build→Verify→Deploy pipeline on a task.
---
Use the pipeline skill. Respect the RISK MATRIX & AUTOMODE in CLAUDE.md (HIGH+ rows: final
approval is mine). Pick reviewers per the orchestration skill (risk-based). End by
writing _reports/runs/<dated>.md + latest.json, then run ./scripts/quality-gate.sh
and show the result. Stop and ask me on any RED, anything irreversible, or scope drift.

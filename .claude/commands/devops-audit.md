---
description: Run the devops skill now — live model-lineup check + instruction audit + harness health.
---
Use the devops skill regardless of the banner. Check the CURRENT model lineup and
fetch the CURRENT vendor prompting guides (never quote them from memory). Triage
CLAUDE.md / rules / skills / agents against them, present proposed cuts and edits
as a diff for my veto (CORE changes need a Kernel Change Rationale + re-baseline
via ./scripts/core-baseline.sh). Run `bash scripts/test-hooks.sh`. After my OK and
a survival test on one normal task, update `.agent/state/model-audit.md` (Last
audited + one log line) and write a dated report per the artifact-versioning rule.
